spring:
  datasource:
    username: postgres
    password: Password123.
    url: jdbc:postgresql://localhost:5432/project_smis
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
